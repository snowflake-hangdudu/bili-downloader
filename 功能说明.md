# B站视频下载助手 — 功能与能力说明

> 版本：1.1.3（开发验收版）
> 平台：Microsoft Edge / Google Chrome / Firefox（Manifest V3）
> 定位：在 B 站普通视频页和 `/list/` 列表页保存视频，仅供个人学习使用

---

## 1. 产品概述

**B站视频下载助手**是一款浏览器扩展。打开 B 站普通视频页后，可通过页面右下角悬浮面板选择清晰度和格式；进入 `/list/` 页面后，可在「单视频 / 列表下载」两个标签之间切换。视频轨和音频轨可在浏览器内合并为可直接播放的 **MP4**；合成默认在专用 Worker 分段执行，合成阶段仍可取消任务或关闭面板。

- 完全免费，无内购、无激活码  
- 不收集、不上传任何用户数据  
- 界面以中文为主（`zh_CN`），商店侧提供英文扩展名/描述  

---

## 2. 核心能力一览

| 能力 | 说明 |
|------|------|
| 视频页识别 | 自动识别当前打开的 BV / av 普通视频 |
| 信息展示 | 封面、标题、UP 主、分 P、可用清晰度等 |
| 清晰度选择 | 按接口真实可用档位展示；记忆“所选”或“始终最高可用”策略 |
| 格式选择 | MP4 视频 / M4A 音频（仅声音，体积更小） |
| 单集下载 | 选定清晰度后下载；下载中可再点「再下一个」并行启动（最多 3 路） |
| 分 P 队列 | 同一 BV 多 P 自动顺序下载；当前 P 保存完成后自动开始下一 P；单项失败自动重试 1 次；可「取消整队」；失败短提示 |
| 列表下载 | 支持 `/list/…` 页面；读取 B 站页面当前已加载的视频；每次最多选择 10 个 |
| 任务中心 | 查看本页下载中、完成、失败、取消任务；失败单视频可直接重试，完成项可打开浏览器下载内容 |
| 诊断日志 | 保留最近 100 条关键日志；一键复制时隐藏媒体地址与敏感参数，并列出任务阶段耗时 |
| 封面下载 | 单独保存当前视频封面；自动升级为 HTTPS 并生成安全文件名 |
| 文件名规则 | 文件名规则可选、可预览并清理非法字符 |
| 下载历史 | 记录最近 50 条；同集 MP4 与故意下的 M4A 可并列；同格式去重；可跳回重下 |
| 会话缓存 | 同 URL + 分 P 约 10 秒内重复开面板复用信息，减少请求 |
| 进度反馈 | 每个任务独立进度卡（百分比 + 阶段文字） |
| 下载控制 | 每个任务可单独暂停 / 继续 / 取消；分 P 队列可整队取消 |
| 体积预估 | 下载前展示预计大小（以接口估算为准） |
| 登录提示 | 未登录或权限不足时给出清晰度相关提示 |
| 反馈入口 | 邮箱 `hangdudu0@agent.qq.com`；面板/弹窗可跳转常见问题 |
| 公告缓存 | 公告与开发合作内容缓存 12 小时；缓存有效时不重复请求接口，接口失败时优先使用已有缓存 |

---

## 3. 功能详解

### 3.1 适用页面

**支持**

- `https://www.bilibili.com/video/BV…`  
- `https://www.bilibili.com/video/av…`  
- 对应移动站 `m.bilibili.com/video/…`  
- `https://www.bilibili.com/list/…`
- `https://bilibili.com/list/…`

**不支持**

- 番剧 / 影视剧集页（`/bangumi/…` 等）  
- 需会员解锁、付费点播等需绕过权限的内容  
- 非 `/list/` 结构的合集 / 系列批量下载

> **分 P、`/list/` 列表与其他合集结构并不相同**
> - **分 P**：同一 BV 的 P1 / P2 / … → **支持**队列下载  
> - **`/list/` 列表**：可在插件面板内继续加载多个 BV → **支持**每次选择最多 10 个下载
> - **其他合集结构**：不在 `/list/` 页面中的系列 → 当前只下载正在打开的单个视频

### 3.2 下载与合并

- 从 B 站播放相关接口获取流地址（与页面播放同源环境，避免 CDN 403）  
- 高清多为 DASH：分别拉取视频轨、音频轨，浏览器内合并为 MP4  
- 低清可能为整段流（durl），直接保存  
- 输出格式：**MP4**（视频）或 **M4A**（仅音频，取 DASH 音频轨直接保存，不转码）

### 3.3 清晰度

- 选项来自接口真实返回，不编造「假 1080P / 4K」  
- 更高清晰度通常需要登录或具备相应账号权限  
- 面板以胶囊按钮展示可选档位，并显示预计体积说明
- 可选择“使用所选清晰度”或“始终最高可用”；后者会按每个视频实际可用档位选择最高项

### 3.4 下载过程控制

| 操作 | 行为 |
|------|------|
| 暂停 | 中断当前网络请求，保留已下载字节 |
| 继续 | 使用 HTTP Range 断点续传 |
| 取消 | 中止任务并重置下载状态 |
| 合并中 | 在专用 Worker 中执行，显示已处理体积与预计剩余时间；此阶段不提供暂停，但可取消任务或关闭面板 |

DASH 场景下，音视频可在后台并行拉取；界面按阶段展示（如先视频流、后音频流，再合并）。

### 3.5 分 P 队列

- 当视频有多个传统分 P（`pages`）时，显示「队列下载全部分 P」
- 按 P1 → P2 → … 自动顺序下载；当前 P 保存完成后自动开始下一 P，并汇总成功数量
- 某一 P 失败时自动等待约 1 秒再重试 1 次（用户取消除外）
- 队列进行中可点「取消整队」：停止后续分 P，并取消进行中的任务
- 与「再下一个」并行下载互斥：并行未结束时请先完成再开分 P 队列
- 单 P 视频不显示队列按钮

### 3.6 `/list/` 列表下载

- 仅在 `/list/…` 页面显示「单视频 / 列表下载」标签
- 列表页仍可下载当前正在展示的单个视频或其封面
- 列表下载固定为 **MP4 视频**，可选择清晰度
- 每次最多勾选 10 个视频，不提供全选，避免浏览器内存和下载链路过载
- 复用单视频下载与合并核心；每个任务显示进度，并支持暂停 / 继续 / 取消
- 下载队列可整队取消；完成后分别统计成功、失败和已取消数量
- 长列表请先向下滚动 B 站页面本身的列表，再在插件面板点击“刷新”读取新增视频
- 面板保持清晰度、列表标题/刷新、可滚动清单和下载按钮的简洁结构；刷新列表会清空当前选择
- 目标清晰度不可用时明确标记清晰度降级；可只重试失败视频

### 3.7 下载历史

- 每次成功下载自动记录（最近 50 条，本地存储，不上传）  
- 工具栏 popup 的「下载历史」可查看：标题、时间、清晰度 / 格式
- 点击「打开」跳回对应视频页（自动定位分 P）并自动展开面板，便于重下  
- 可一键「清空」

### 3.8 其他体验

- 悬浮按钮可**按住拖拽**到任意位置（避免遮挡播放器控件），位置本地记住；窗口缩放后自动夹回可视区  
- 站内切换视频（合集 / 推荐跳转）**即时刷新**面板，无需等待  
- 文件名可选择标题、BV 号、清晰度，或包含 UP 主 / 分 P 的完整规则；保存前会清理 Windows 非法字符并显示预览
- 下载历史与按钮位置均存本地（`storage` 权限），**不上传任何数据**  
- 合并库按需加载（仅检测到高清 DASH 片源时注入），页面性能开销最小  

---

## 4. 使用入口

### 4.1 页面悬浮面板（主入口，实际下载在此完成）

位置：视频页右下角浮动按钮 → 展开面板。

面板大致包含：

1. 视频识别 / 标题信息  
2. 封面与 UP 等信息卡片  
3. 分 P 列表（多 P 时）  
4. 清晰度选择 + 清晰度策略 + 格式选择（MP4 视频 / M4A 音频）
5. 预计大小、登录相关提示  
6. 「开始下载」；多 P 时另有「队列下载」  
7. 「下载封面」
8. 下载中的进度区（阶段、百分比、进度条、暂停 / 取消）
9. 底栏：常见问题、隐私政策、公告、开发合作、诊断日志、任务中心、反馈邮箱

在 `/list/` 页面还会显示双标签：

- **单视频**：与普通视频页一致，下载当前展示的视频或封面
- **列表下载**：选择清晰度和最多 10 个当前已加载的视频，查看队列进度和整队取消

### 4.2 工具栏弹窗（信息入口）

点击扩展图标打开 popup：

- **在视频页**：预览封面、标题、UP、可用清晰度，并可「打开下载面板」；文案说明 MP4/M4A 与最多 3 路并行  
- **列表页**：可直接打开页面内的「列表下载」标签
- **其他页面**：明确提示仅支持 `/video/` 与 `/list/`；首页、搜索和番剧页不提供下载入口
- **下载历史**：点「打开」跳回对应视频（定位分 P）并自动展开面板（与页面历史一致）  
- **加载失败**：错误提示 + 刷新重试  

> 弹窗只负责查看信息与唤起面板，**不会在弹窗内直接发起下载**。

---

## 5. 界面与体验

- 黑白极简主题面板 / 弹窗，跟随系统深浅色（亮色=黑、深色=白，无彩色）
- 加载时有骨架屏，减少空白等待感  
- 失败时给出简短状态文案，并可跳转 FAQ 对应说明（如 CDN 403 等）  
- 列表页的两个标签共用稳定面板外框；视频清单、单视频任务和列表任务各自按空间滚动
- 面板打开时悬浮入口仍保留；悬浮按钮只负责打开，面板仅由右上角 X 关闭，避免误触隐藏任务交互

---

## 6. 隐私与合规

| 项目 | 说明 |
|------|------|
| 数据收集 | 不收集、不上传浏览记录、账号或下载内容 |
| 权限 | `activeTab`、`scripting`、`storage`、`downloads`；主机权限限于 bilibili / bilivideo / hdslb 与公告配置域名 |
| 用途声明 | 个人学习备份；请遵守 B 站用户协议与版权法规 |
| 不做事项 | 不破解会员、不绕过付费、不批量扒合集 / 番剧站 |

相关页面：

- 隐私政策：https://snowflake-hangdudu.github.io/bili-downloader/  
- 常见问题：https://snowflake-hangdudu.github.io/bili-downloader/faq.html  

---

## 7. 能力边界（明确不做）

以下能力**当前版本不提供**，避免与已有功能混淆：

- 番剧页下载 / VIP 解锁  
- 非 `/list/` 结构的合集一键批量
- 字幕、弹幕导出
- 自定义输出目录（跟随浏览器下载设置）  
- 设置页、账号体系、云同步  

---

## 8. 技术能力摘要（给开发者）

| 要点 | 说明 |
|------|------|
| 架构 | MV3；`page-agent.js`（MAIN）负责 API / CDN / 合并；`content.js`（ISOLATED）负责 UI |
| 为何 MAIN | 扩展后台直连 CDN 易 403，须与页面共享 Cookie / Referer |
| 合并 | `mp4-remux`（MIT）将 DASH 音视频封装为 MP4 |
| 通信 | 页内 `postMessage`；popup ↔ content 用 `chrome.runtime` |
| 公告配置 | 后台仅访问白名单配置地址；本地缓存 12 小时，缓存有效时不重复请求 |
| 打包 | `python scripts/pack.py` → `bilibili-downloader.zip` |

更完整的架构、协议与改动指引见仓库内 **[DEVELOPER.md](./DEVELOPER.md)**。

---

## 9. 快速开始

1. 打开 `edge://extensions` 或 `chrome://extensions`  
2. 开启「开发者模式」→「加载解压缩的扩展」→ 选择本扩展目录  
3. 打开任意 B 站普通视频页或 `/list/` 列表页
4. 点击右下角悬浮按钮 → 选择下载模式和清晰度 → 开始下载

反馈邮箱：`hangdudu0@agent.qq.com`  

仓库：https://github.com/snowflake-hangdudu/bili-downloader  
