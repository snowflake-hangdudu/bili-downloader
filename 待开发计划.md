# B站视频下载助手 — 待开发计划

> 当前版本：1.1.5（开发验收版，重载后验收）
> 计划更新时间：2026-09-07
> 原则：先稳定下载和任务交互，再扩展列表能力；不以堆叠按钮代替产品设计。

## 一、当前基础

当前已经具备：

- 普通视频 MP4 / M4A 下载、真实清晰度、预计大小
- 单任务及最多 3 路并行任务的暂停、继续、取消
- 多分 P 队列、失败重试 1 次、整队取消
- `/list/` 页面双标签、最多选择 10 个视频、列表 MP4 下载
- 当前视频封面下载
- 最近 50 条本地历史
- 公告 / 开发合作 12 小时缓存和接口失败回退
- 悬浮按钮拖动、B 站 SPA 页面切换适配
- 桌面与紧凑窗口布局回归测试

当前边界：

1. 任务中心和诊断日志为当前页面会话数据；刷新页面不会伪造“恢复中”的下载状态。
2. 浏览器下载文件的真实路径由浏览器管理，任务中心提供“打开下载内容”以便定位。
3. 真实 Edge 中的网络、下载、通知和布局验收仍需在扩展重载后完成；本地语法与回归检查不能替代它。

## 当前开发状态（2026-09-01）

### 1.1.5 — 下载可靠性与性能审查

- 已实现双轨暂停共享等待、取消合成、暂停期间禁止发起下载；队列固定来源 URL，避免切视频后混入其他分 P。
- MP4 / M4A 统一浏览器原生保存，查询真实完成状态后继续下一项；保存失败明确报错，成功提示附浏览器下载记录入口。
- 续传返回整文件时重新开始，校验 Content-Range 与已知总长度；停止未完整文件的成功保存。
- 不再将多段旧格式的第一段冒充完整 MP4，不再静默保存缺失音轨的 M4S。
- 同清晰度优先 AVC，音轨优先 AAC；合成前检查片段索引，减少不支持封装导致的无限等待。
- 大文件 Worker 不可用时停止，不回退页面主线程合成；CDN 探测及时中断响应体。
- 设计评估：保留现有布局与品牌，建议后续只借用参考图的配色层级，不直接换成装饰型风格。本次仅改善文字对比度、减少动态效果支持。
- 详细证据、验收边界和剩余项见 `docs/1.1.5-audit.md`。未打包，未宣称商店反馈已经全部复现修复。

### 1.1.4 — 分 P 队列总控

- 分 P 与列表队列均提供「暂停全部 / 继续全部 / 取消整队」；暂停时不会启动后续任务；
- 合成阶段不伪造暂停能力，保留取消整队；取消后不会自动恢复队列。

### 1.1.3 — 分 P 自动顺序队列

- 点击一次「队列下载全部分 P」后，按 P1 → P2 → … 自动继续，无需用户重复点击；
- 当前 P 成功保存或完成其失败重试后，才会开始下一 P；
- 所有分 P 队列固定单任务执行，避免长合集同时争用 CDN、下载槽位和内存。

### 1.1.2 — 大文件合成与并行热修复

- DASH 音视频合成默认在专用 Worker 中以 4 MB 分段流式处理，不再先将两条大文件完整复制为 `ArrayBuffer`；
- Worker 不可用时才回退到可取消的分段主线程合成；浮窗关闭与取消操作保持可用；
- 合成状态显示已处理体积和预计剩余时间；合成阶段保留取消按钮；
- 超过 512 MB 的预估任务独占下载通道；DASH 分 P 队列改为串行，避免多个大文件叠加占满内存；
- 下载与合成不再使用 10 分钟请求超时。

1.1.0 的源码实现与本地回归检查已完成，待扩展重载后的真实页面验收：

- 单视频与列表分别拥有固定的清晰度、任务卡和整队取消区域，切换 Tab 不再搬运 DOM；
- 下载任务统一记录状态、尝试次数、失败类型和更新时间；
- 单视频失败后可直接“重试此任务”；取消不会再记作失败；
- 列表固定 MP4，保留清晰度选择；结束状态会显示成功、失败、取消及首个失败原因；
- 增加任务 UI 与布局回归检查，覆盖任务区域隔离、重试入口和列表 MP4 约束。

仍待验收：真实 Edge 页面中的下载、暂停、取消、重试和分 P 队列；验收通过后才可标记发布完成。

1.2.0 的源码实现与本地回归检查已完成，待扩展重载后的真实页面验收：

- 当前简洁列表读取 B 站页面已加载的视频；长列表需先滚动 B 站页面后点击“刷新”；
- 面板保留清晰度、列表标题/刷新、可滚动清单和下载按钮；始终限制最多选择 10 个。
- 目标清晰度不可用时，任务卡、历史记录和完成汇总会明确标记清晰度降级。
- 列表任务结束后可仅重试失败视频，沿用失败时的目标清晰度；成功和取消项不会重复下载。

1.3.0 的源码实现与本地回归检查已完成，待扩展重载后的真实页面验收：

- 面板内新增“诊断日志”，保留本页最近 100 条关键事件；
- 支持一键复制诊断报告，报告会隐藏完整地址和常见敏感参数；
- 下载、列表、配置和页面代理日志继续同步输出至开发者工具，便于深度排查。

1.4.0 的源码实现与本地回归检查已完成，待扩展重载后的真实页面验收：

- 记忆格式、清晰度与“所选 / 始终最高可用”策略；列表和单视频使用同一策略；
- 文件名规则支持标题、BV 号、清晰度，及包含 UP 主、分 P 的完整规则；保存前预览并清理非法字符；
- 补充 Esc 从公告/任务页返回下载主页、Tab 原生焦点、ARIA 选中态、焦点样式与窄窗口布局回归检查；面板仅由右上角 X 关闭。

本地检查命令：

```powershell
node --check content/content.js
node --check content/page-agent.js
node --check background.js
node test/task-ui-regression.test.mjs
node test/list-pagination-regression.test.mjs
node test/layout-regression.test.mjs
git diff --check
```

## 二、版本路线

### 1.1.0 — 统一任务与界面架构（最高优先级）

目标：解决两个标签串状态、样式反复回归和任务组件归属不清的问题。

开发内容：

- 建立统一 `DownloadTask` 模型：等待、解析、下载、暂停、合并、完成、失败、取消
- 单视频、分 P、列表下载统一调用同一个任务引擎
- 清晰度、格式、预计大小不再通过 `insertBefore` 在两个标签之间移动
- 下载任务区成为独立共享区域，两个标签只负责选择下载目标
- 统一失败类型：网络失败、权限不足、清晰度不可用、合并失败、保存失败、用户取消
- 增加单任务“重试”和失败原因详情
- 继续保留失败与取消的独立统计

验收标准：

- 连续切换两个标签 50 次，无内容串页、宽高跳动或任务丢失
- 普通 `/video/` 页面不受 `/list/` 样式影响
- 单视频、分 P、列表的任务卡交互一致
- 失败、取消、完成三种结果准确区分
- 下载中关闭再打开面板，任务状态仍能正确显示

预计工作量：4—6 个开发日。

### 1.2.0 — 完整列表浏览与批量体验

目标：用户无需先滚动 B 站页面，也能在插件中浏览和选择列表视频。

开发内容：

- 调用列表分页接口，按页加载完整数据
- 面板内提供继续加载 / 自动懒加载，不一次性渲染数千条
- 标题关键词搜索
- 发布时间正序 / 倒序
- “全部 / 已选择”筛选
- 翻页、搜索和切换标签时保留已选项目
- 继续限制每次最多 10 个，不恢复全选
- 显示每个视频最终采用的实际清晰度
- 目标清晰度不可用时，明确提示该视频发生了清晰度降级
- 支持“仅重试失败视频”

验收标准：

- 无需滚动 B 站主页面即可继续读取列表
- 数千条列表不会造成面板卡顿
- 搜索、排序、分页后选择状态不丢失
- 任意时刻最多选择 10 个
- 成功、失败、取消和降级结果可以逐项查看

预计工作量：4—5 个开发日。

### 1.3.0 — 任务中心与诊断能力

目标：让用户知道任务发生了什么，并能自行处理常见失败。

开发内容：

- 增加本次会话任务中心，保留最近任务的状态和结果
- 提供“全部 / 下载中 / 已完成 / 失败 / 已取消”筛选
- 完成项提供“打开文件”和“打开下载文件夹”
- 失败项提供重新下载
- 增加面板内调试日志页，保留最近 100 条关键日志
- 支持一键复制诊断报告
- 诊断报告自动移除 Cookie、签名参数和完整媒体地址
- 增加网络、解析、合并、保存各阶段耗时

验收标准：

- 用户不打开开发者工具也能复制有效诊断信息
- 诊断信息不包含账号凭据和敏感请求参数
- 任务失败后可直接重试，不需要重新选择视频
- 下载完成后能快速定位本地文件

预计工作量：3—5 个开发日。

### 1.4.0 — 偏好设置与无障碍体验

目标：减少重复选择，同时保持默认界面简洁。

开发内容：

- 记忆上次使用的格式和清晰度策略
- 增加“最高可用清晰度”策略，而不是写死某个档位
- 文件名规则：标题、UP 主、BV号、分 P、清晰度
- 文件名保存前预览和非法字符处理
- 下载完成浏览器通知（用户主动开启后才申请通知权限）
- 键盘操作：Tab、Enter、Space、Esc
- 完整焦点样式、ARIA 标签和屏幕阅读器状态播报
- 适配 400px 以下窄窗口和较低高度窗口

验收标准：

- 可选设置不强制填写数字，不增加不必要的运行参数
- 刷新页面后用户偏好仍然有效
- 键盘可以完成打开面板、选择、开始、暂停和取消
- 深色模式、缩放和窄窗口均无裁切

预计工作量：3—4 个开发日。

### 1.5.0 — 可选高级能力

以下功能在核心稳定后再评估：

- M4A 写入标题、UP 主、封面等元数据
- 列表中按已选视频批量下载封面，仍受 10 个上限约束
- 通过 File System Access API 选择保存目录
- 任务元数据恢复：刷新页面后显示未完成任务及恢复建议
- 导出下载结果清单（JSON / CSV）

这些能力不应阻塞 1.1.0—1.4.0。

## 三、质量门槛

每个版本完成前必须通过：

1. JS 语法检查和 `git diff --check`
2. 普通视频、分 P、列表三条主流程测试
3. MP4、M4A、封面三种输出测试
4. 成功、失败、暂停、继续、取消、整队取消测试
5. 两个标签连续切换与 B 站 SPA 路由切换测试
6. 900×700、760×500、窄窗口、深色模式布局测试
7. 扩展重新加载后的真实 Edge 页面验证
8. 下载文件可播放、音视频同步、文件名正确
9. 文档、版本号、权限说明和商店文案同步检查

仅有自动测试通过不能替代真实页面验证；真实验证未执行时必须标记为“待验证”。

## 四、明确不纳入本插件

- 番剧、付费点播、会员权限绕过
- 无限制批量和全选下载
- 自动提高并发以规避平台限制
- 字幕和弹幕提取：继续由独立插件负责，避免产品边界混乱
- 账号体系、云端同步和浏览记录上传

## 五、推荐执行顺序

1. 先完成 1.1.0 的任务模型与 UI 解耦
2. 再完成 1.2.0 的列表分页和失败重试
3. 接着补齐 1.3.0 的任务中心和诊断日志
4. 最后进入 1.4.0 的偏好设置与无障碍优化
5. 根据真实用户反馈决定是否开发 1.5.0

未经明确要求，不自动生成 ZIP / XPI 发布包。
