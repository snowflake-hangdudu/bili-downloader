# 1.1.8 视频页合集下载

普通 `/video/` 页的视频接口提供合集信息时，显示与 `/list/` 页相同的列表下载入口。按合集章节顺序读取有效条目并去重，复用原有串行队列和下载控制，不限制勾选 10 个。

合集入口随当前视频更新，普通非合集视频不显示该入口。合集数据来自当前视频接口，不扫描推荐视频。仅能下载接口返回的有效条目。

验证：24 项本地自动化测试通过；尚未在用户截图对应合集上完成真实浏览器下载验证。本次未打包。
